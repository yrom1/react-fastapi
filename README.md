# react-fastapi
# setup
```
npx create-react-app front
cd front
npm start
```
# kill 3000
```
# Find the process ID of the React app
$ lsof -t -i :3000

# Kill the process using the process ID
$ kill -9 <process_id>
```
